# Tested on NVIDIA RTX 5070, CUDA 12.8+, Python 3.10+
# Install PyTorch separately with CUDA support — see README Installation section
streamlit==1.32.0
yfinance==0.2.40
pandas==2.0.0
requests>=2.31.0
torch==2.0.0
transformers>=4.40.0
huggingface_hub>=0.23.0
numpy==1.26.0
feedparser>=6.0.0
exchange_calendars>=4.5   # optional: accurate NSE/BSE holiday calendar for forecast timestamps (falls back to weekend-only if absent)
